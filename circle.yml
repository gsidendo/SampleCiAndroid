general:
  branches:
    only:
      - master # list of branches to build
      - develop # list of branches to build
      - init # list of branches to build

machine:
  environment:
#    ANDROID_HOME: /usr/local/android-sdk-linux
#    JAVA_OPTS: "-Xmx1048m -XX:MaxPermSize=512m -Duser.language=ja -Duser.country=JP"
  timezone: Asia/Tokyo

dependencies:
#  pre:
#    - echo y | android update sdk --no-ui --all --filter "platform-tools,tools"
#    - echo y | android update sdk --no-ui --filter "android-23,build-tools-23.0.1"
  override:
     - ANDROID_HOME=/usr/local/android-sdk-linux ./gradlew dependencies



test:
  override:
    - chmod +x gradlew
    - ./gradlew check findbugsConvertSJis checkstyleReport
  post:
      - cp -R /home/ubuntu/SampleCiAndroid/build/reports/* $CIRCLE_ARTIFACTS