general:
  branches:
    only:
      - master # list of branches to build
      - develop # list of branches to build
      - init # list of branches to build
#  artifacts:
#     - /home/ubuntu/SampleCiAndroid/build/reports

machine:
  environment:
    ANDROID_HOME: /usr/local/android-sdk-linux



test:
  override:
    - chmod +x gradlew
    - ./gradlew check
  post:
      - cp -R /home/ubuntu/SampleCiAndroid/build/reports/* $CIRCLE_ARTIFACTS
      - cp -R /home/ubuntu/SampleCiAndroid/app/build/outputs/lint/* $CIRCLE_ARTIFACTS/lint/